# Firebase Web 設定（從 Firebase Console → 專案設定 → 你的應用程式 → SDK setup and configuration → Config 複製）
# 複製這個檔成 .env，填入你的值。.env 已被 .gitignore 忽略，不會上傳到 GitHub。
# 注意：這些值會被打包進前端（公開可見）是正常的，資料安全靠 Firestore 安全規則 + 登入控管。
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# （選用）本機測試房間：在 .env.development.local 設 VITE_ROOM_ID=dragon-dev，
# 開發時的打卡／專注／課程進度就會寫進獨立房間，不會動到正式的 shared-room。
# 用 .development.local 而不是 .local，才能保證 npm run build 出來的正式版一定是 shared-room。
